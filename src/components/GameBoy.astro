---
import { getCollection, getEntry } from 'astro:content';

// Fetch Data
const allLinks = await getCollection('links');
const sortedLinks = allLinks.sort((a, b) => a.data.order - b.data.order);
const aboutEntry = await getEntry('sections', 'about');
const aboutText = aboutEntry && aboutEntry.body ? aboutEntry.body.trim() : "";

// Map icons to SVGs (Inline for performance)
// --- 60Hz Quantization Heartbeat ---
// Game Boy Original Refresh Rate: ~59.73Hz (~16.74ms)
// We align all animations to this frame time to prevent jitter.
// Quantized to 60fps (~16.74ms)

const iconSvgs = {
    instagram: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/></svg>`,
    twitter: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M23.953 4.57a10 10 0 01-2.825.775 4.958 4.958 0 002.163-2.723c-.951.555-2.005.959-3.127 1.184a4.92 4.92 0 00-8.384 4.482C7.69 8.095 4.067 6.13 1.64 3.162a4.822 4.822 0 00-.666 2.475c0 1.71.87 3.213 2.188 4.096a4.904 4.904 0 01-2.228-.616v.06a4.923 4.923 0 003.946 4.827 4.996 4.996 0 01-2.212.085 4.936 4.936 0 004.604 3.417 9.867 9.867 0 01-6.102 2.105c-.39 0-.779-.023-1.17-.067a13.995 13.995 0 007.557 2.209c9.053 0 13.998-7.496 13.998-13.985 0-.21 0-.42-.015-.63A9.935 9.935 0 0024 4.59z"/></svg>`,
    linkedin: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>`,
    goodreads: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M11.43 23.995c-3.608-.208-6.274-2.077-6.448-5.078.695.007 1.375-.013 2.07-.006.224 1.342 1.065 2.43 2.683 3.026 1.558.496 3.085.393 4.571-.322 1.298-.768 1.914-1.795 2.15-3.219H16.4c-.023.278-.074.958-.106 1.09-1.284 2.088-3.587 3.226-6.117 3.226-.013 0-.026-.003-.04-.003-.353 0-.704-.016-1.053-.035l.346-18.68h2.07l-.03 5.59c.672-.804 1.604-1.378 2.768-1.572 1.463-.242 2.851.04 4.063.88 1.227.85 2.02 2.023 2.398 3.453.4 1.514.357 3.013-.173 4.47-.574 1.57-1.634 2.717-3.189 3.396-1.098.479-2.253.62-3.443.505-1.118-.108-2.109-.515-2.971-1.22-.013.01-.02.018-.028.028-.51.698-1.19 1.203-2.024 1.47-.83.26-1.68.28-2.526.082-1.234-.286-2.21-.936-2.86-2.028-.63-1.063-.804-2.211-.514-3.4.28-1.144.91-2.072 1.89-2.732.964-.65 2.033-.877 3.18-.688 1.13.186 2.07.707 2.772 1.58l.017-.025.03-5.595-2.07.018-.031 7.59c-.014.71-.245 1.262-.736 1.682-.49.42-1.086.612-1.783.574-.697-.038-1.273-.306-1.714-.791-.442-.486-.632-1.065-.58-1.775.06-.817.386-1.47.987-1.96.568-.463 1.227-.672 1.976-.623.6.039 1.105.232 1.534.597-.004-.043-.006-.082-.007-.123-.01-.173-.02-.345-.03-.517-.03-.535-.06-1.07-.09-1.605-.03-.535-.06-1.07-.089-1.603-.018-.313-.035-.627-.053-.94l-2.07.006.034 1.787c-.672-.91-1.578-1.466-2.768-1.628-1.24-.17-2.392.08-3.456.73-1.3.798-2.114 1.955-2.43 3.457-.346 1.647-.02 3.166.93 4.483.933 1.293 2.206 2.047 3.81 2.217z"/></svg>`,
    email: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M24 5.457v13.909c0 .904-.732 1.636-1.636 1.636h-3.819V11.73L12 16.64l-6.545-4.91v9.273H1.636A1.636 1.636 0 0 1 0 19.366V5.457c0-2.023 2.309-3.178 3.927-1.964L5.455 4.64 12 9.548l6.545-4.91 1.528-1.145C21.69 2.28 24 3.434 24 5.457z"/></svg>`,
    playstation: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M8.985 2.596v17.548l3.915 1.261V6.688c0-.69.304-1.151.794-.991.636.181.76.814.76 1.505v5.876c2.441 1.193 4.362-.002 4.362-3.153 0-3.237-1.126-4.675-4.438-5.827-1.307-.448-3.728-1.186-5.393-1.502zm5.728 15.029-9.732-3.326c-1.085-.353-1.231-.787-.326-1.001.83-.197 2.334-.149 3.433.197l4.556 1.621v-2.539l-.259-.091c-1.449-.494-2.983-.724-4.572-.624-2.072.16-4.036.781-4.036.781-2.129.738-2.386 1.787-1.129 2.604 1.109.712 3.018 1.248 5.328 1.536v2.842l1.607-.561s-3.236-1.164-4.418-1.6c-.96-.358-1.101-.788-.31-1.004.731-.199 2.074-.196 3.396.161l4.462 1.593v2.411z"/></svg>`,
    github: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>`
};
---

<main class="console">
    <div class="console-body">
        
        <div class="power-switch-container">
            <div class="switch-track" id="power-switch" role="switch" aria-checked="true" tabindex="0">
                <div class="switch-handle"></div>
            </div>
        </div>

        <div class="left-controls">
            <div class="dpad-area">
                <div class="dpad">
                    <button id="up" class="dpad-btn dpad-up" aria-label="Up"></button>
                    <button id="right" class="dpad-btn dpad-right" aria-label="Right"></button>
                    <button id="down" class="dpad-btn dpad-down" aria-label="Down"></button>
                    <button id="left" class="dpad-btn dpad-left" aria-label="Left"></button>
                    <div class="dpad-center"></div>
                </div>
            </div>
        </div>

        <section class="screen-lens">
            <div class="power-indicator">
                <span class="led"></span>
            </div>
            <div class="screen-display" id="main-screen">
                <div id="start-overlay">
                    <div class="start-content">
                        <h2>SYSTEM BOOT</h2>
                        <p class="blink">PRESS ANY BUTTON OR CLICK ANYWHERE</p>
                    </div>
                </div>
                <div id="boot-screen" class="boot-screen">
                    <div class="progress-container">
                        <div class="progress-bar"></div>
                    </div>
                </div>

                <div class="top-hud" id="top-hud" aria-label="Status HUD">
                    <div class="hud-item" id="hud-time">--:--</div>
                    <div class="hud-item" id="hud-date">-- ---</div>
                    <div class="hud-item mute-icon" id="hud-mute" style="display:inline-block;width:1.5em;height:1.5em;vertical-align:middle;position:relative;top:0.1em;visibility:hidden" aria-label="Muted">
                        <svg viewBox="0 0 24 24" fill="currentColor" width="1em" height="1em" style="display:block;margin:auto;">
                            <path d="M16.5 12c0-1.77-1.02-3.29-2.5-4.03V5a1 1 0 0 0-1.62-.78l-3.38 2.78H5a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h4l3.38 2.78A1 1 0 0 0 13 19v-2.97c1.48-.74 2.5-2.26 2.5-4.03zm-2.5 2.03V17l-3.38-2.78A1 1 0 0 0 9 13H5v-6h4a1 1 0 0 0 .62-.22L13 5v2.97c-1.48.74-2.5 2.26-2.5 4.03s1.02 3.29 2.5 4.03zM19.78 4.22a.75.75 0 0 0-1.06 0l-14 14a.75.75 0 1 0 1.06 1.06l14-14a.75.75 0 0 0 0-1.06z"/>
                        </svg>
                    </div>
                </div>
                
                <div class="battery" id="battery-indicator">
                    <div class="battery-level" id="battery-level"></div>
                </div>

                <div class="tabs-header" role="tablist" aria-label="Profile sections">
                    <button type="button" class="tab-indicator active" id="tab-ind-0" role="tab" aria-selected="true" aria-controls="tab-0">LINKS</button>
                    <button type="button" class="tab-indicator" id="tab-ind-1" role="tab" aria-selected="false" aria-controls="tab-1">ABOUT</button>
                    <button type="button" class="tab-indicator" id="tab-ind-2" role="tab" aria-selected="false" aria-controls="tab-2">HELP</button>
                </div>

                <div class="tab-container">
                    <div class="tab-content active" id="tab-0" role="tabpanel" aria-labelledby="tab-ind-0">
                        <nav class="links" aria-label="Social links">
                            {sortedLinks.map((link: any, index: number) => (
                                <a href={link.data.url} 
                                   target={link.data.url.startsWith('mailto') ? '_self' : '_blank'} 
                                   rel={link.data.url.startsWith('mailto') ? '' : 'noopener noreferrer'}
                                   class={`social-link ${link.data.icon} ${index === 0 ? 'active' : ''}`}
                                   data-index={index}>
                                    <Fragment set:html={iconSvgs[link.data.icon as keyof typeof iconSvgs] || ''} />
                                    <span class="link-text">{link.data.title}</span>
                                </a>
                            ))}
                        </nav>
                    </div>
                    <!-- Pass text to data attribute for JS to find -->
                    <div class="tab-content" id="tab-1" role="tabpanel" aria-labelledby="tab-ind-1" hidden>
                        <div class="about-text" id="about-text-content" data-text={aboutText}>
                            <!-- Text will be typed here by JS -->
                        </div>
                    </div>
                    <div class="tab-content" id="tab-2" role="tabpanel" aria-labelledby="tab-ind-2" hidden>
                        <section class="help-panel" aria-label="Controls and secrets">
                            <h3>KEYMAP</h3>
                            <ul>
                                <li><span>ARROWS / WASD</span><span>NAVIGATE</span></li>
                                <li><span>A / ENTER / Z</span><span>OPEN LINK</span></li>
                                <li><span>B / X</span><span>SWITCH THEME</span></li>
                                <li><span>SELECT / Q</span><span>SWITCH THEME</span></li>
                                <li><span>START</span><span>OPEN HELP TAB</span></li>
                                <li><span>LEFT / RIGHT</span><span>CHANGE TAB</span></li>
                                <li><span>M</span><span>MUTE AUDIO</span></li>
                                <li><span>H</span><span>OPEN HELP TAB</span></li>
                            </ul>
                            <p class="secret-hint">SECRET: U U D D L R L R B A</p>
                        </section>
                    </div>
                </div>

                <footer class="footer">
                    <p>MADE WITH &lt;3</p>
                </footer>
            </div>
            <div class="logo">GAME BOY <span class="advance">ADVANCE</span></div>
        </section>

        <div class="right-controls">
            <div class="action-buttons">
                <div class="btn-group group-a">
                    <button type="button" class="btn-a" id="btn-a">A</button>
                </div>
                <div class="btn-group group-b">
                    <button type="button" class="btn-b" id="btn-b">B</button>
                </div>
            </div>
        </div>

        <div class="meta-controls">
            <div class="meta-group">
                <button class="pill-btn select" id="btn-select" aria-label="Select"></button>
                <span class="pill-label label-select">SELECT</span>
            </div>
            <div class="meta-group">
                <button class="pill-btn start" id="btn-start" aria-label="Start"></button>
                <span class="pill-label label-start">START</span>
            </div>
        </div>

        <div class="speaker" aria-hidden="true">
            <span style="view-transition-name: s1"></span>
            <span style="view-transition-name: s2"></span>
            <span style="view-transition-name: s3"></span>
            <span style="view-transition-name: s4"></span>
            <span style="view-transition-name: s5"></span>
            <span style="view-transition-name: s6"></span>
        </div>
    </div>
</main>
<div class="app-turn-control">
    <button type="button" class="app-btn turn" id="btn-turn" aria-label="Toggle Layout">
        <svg viewBox="0 0 24 24" class="turn-icon">
            <path d="M16.48 2.52c3.27 1.55 5.61 4.72 5.97 8.48h1.5C23.44 4.84 18.29 0 12 0l-1 0v6l5.48-3.48zM4.93 1.25c-2.72 1.96-4.59 5.05-4.86 8.75h1.52c.26-3.13 1.83-5.75 4.14-7.41l-0.8-1.34zM1.55 13H0.05C0.38 18.66 4.92 23.16 10.5 23.5L10.5 22c-4.75-0.3-8.58-4.14-8.95-9zM19.93 13h1.52c-0.27 3.7-2.14 6.79-4.86 8.75l-0.8-1.34c2.31-1.66 3.88-4.28 4.14-7.41zM2 13h2c0-4.42 3.58-8 8-8s8 3.58 8 8h2c0-5.52-4.48-10-10-10S2 7.48 2 13z" fill="currentColor"/>
        </svg>
    </button>
</div>

<!-- Inline Script to preserve interactivity -->
<script is:inline>
    'use strict';

    const FRAME_DURATION = 16.74;

    // --- Audio Engine ---
    function updateMuteIcon() {
        const muteIcon = document.getElementById('hud-mute');
        if (!muteIcon) return;
        muteIcon.style.visibility = SoundEngine.muted ? 'visible' : 'hidden';
    }

    const SoundEngine = {
        ctx: null, // For SFX
        muted: localStorage.getItem('gb_muted') === 'true',
        
        init: function() {
            // Init SFX Context
            if (!this.ctx) {
                // Use bracket notation to avoid TypeScript checking for webkitAudioContext
                const AudioCtx = window.AudioContext || window['webkitAudioContext'];
                this.ctx = new AudioCtx();
            } else if (this.ctx.state === 'suspended') {
                this.ctx.resume();
            }
        },
        
        toggleMute: function() {
            this.muted = !this.muted;
            localStorage.setItem('gb_muted', this.muted);
            updateMuteIcon();
            return this.muted;
        },

        playTone: function(freq, type, duration, vol = 0.1) {
            if (!this.ctx || this.muted) return;
            
            // Ensure context is running (sometimes browsers suspend it)
            if (this.ctx.state === 'suspended') this.ctx.resume();
            
            try {
                const osc = this.ctx.createOscillator();
                const gain = this.ctx.createGain();
                
                osc.type = type;
                osc.frequency.setValueAtTime(freq, this.ctx.currentTime);
                
                gain.gain.setValueAtTime(vol, this.ctx.currentTime);
                gain.gain.exponentialRampToValueAtTime(0.01, this.ctx.currentTime + duration);
                
                osc.connect(gain);
                gain.connect(this.ctx.destination);
                
                osc.start();
                osc.stop(this.ctx.currentTime + duration);
            } catch(e) { console.error('Audio error', e); }
        },
        
        hover: function() {
            // High-pitched blip (6 frames)
            this.playTone(440, 'sine', (FRAME_DURATION * 6) / 1000, 0.05);
        },
        
        select: function() {
            // Chime-like ding (Square wave)
            // 6 frames then 12 frames
            this.playTone(660, 'square', (FRAME_DURATION * 6) / 1000, 0.05);
            setTimeout(() => this.playTone(880, 'square', (FRAME_DURATION * 12) / 1000, 0.05), FRAME_DURATION * 6);
        },
        
        back: function() {
            // Low-pitched buzz (9 frames)
            this.playTone(150, 'sawtooth', (FRAME_DURATION * 9) / 1000, 0.05);
        },
        
        switch: function() {
            // Tab switch sound (6 frames)
            this.playTone(300, 'triangle', (FRAME_DURATION * 6) / 1000, 0.05);
        }
    };

    // Color Themes
    const themes = ['theme-atomic', 'theme-berry', 'theme-ice', 'theme-jungle', 'theme-classic', 'theme-sunblaze', 'theme-cyberpunk', 'theme-midnight'];
    
    // Randomize start index or load from storage
    let currentThemeIndex = (() => {
        try {
            const saved = localStorage.getItem('gb_theme');
            if (saved && themes.includes(saved)) return themes.indexOf(saved);
        } catch(e) {}
        return Math.floor(Math.random() * themes.length);
    })();

    let currentIndex = 0;
    let currentTab = 0; // 0 = Links, 1 = About
    const numTabs = 3;
    let hudClockInterval;
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');

    function getScrollBehavior() {
        return prefersReducedMotion.matches ? 'auto' : 'smooth';
    }

    function updateTabUI() {
        document.querySelectorAll('.tab-indicator').forEach((indicator, index) => {
            const isActive = index === currentTab;
            indicator.classList.toggle('active', isActive);
            indicator.setAttribute('aria-selected', String(isActive));
            indicator.tabIndex = isActive ? 0 : -1;
        });

        document.querySelectorAll('.tab-content').forEach((content, index) => {
            const isActive = index === currentTab;
            content.classList.toggle('active', isActive);
            content.hidden = !isActive;

            if (index === 1) {
                if (isActive) startTypewriter();
                else stopTypewriter();
            }
        });
    }

    function initHudClock() {
        const timeEl = document.getElementById('hud-time');
        const dateEl = document.getElementById('hud-date');
        if (!timeEl || !dateEl) return;

        const updateHudClock = () => {
            const now = new Date();
            const timeStr = now.toLocaleTimeString([], {
                hour: '2-digit',
                minute: '2-digit',
                hour12: false
            });
            const dateStr = now.toLocaleDateString([], {
                day: '2-digit',
                month: 'short'
            }).toUpperCase();

            timeEl.textContent = timeStr;
            dateEl.textContent = dateStr;
        };

        // Update clock every second (approx 60 frames)
        updateHudClock();
        if (hudClockInterval) clearInterval(hudClockInterval);
        hudClockInterval = setInterval(updateHudClock, FRAME_DURATION * 60);
    }

    function switchTab(direction) {
        // Only switch if we have multiple tabs
        if (direction === 'left') {
            currentTab = (currentTab - 1 + numTabs) % numTabs;
        } else {
            currentTab = (currentTab + 1) % numTabs;
        }

        // Play sound
        SoundEngine.switch();

        // Reset scroll position of the main screen
        const screen = document.getElementById('main-screen');
        if (screen) {
            screen.scrollTo({ top: 0, behavior: 'auto' });
        }

        updateTabUI();

        // If switching back to Links tab, ensure active link is visible
        if (currentTab === 0) {
            // Short delay to allow tab content to become visible
            setTimeout(() => updateActiveLink(currentIndex), FRAME_DURATION * 3); // ~50ms
        }
    }

    function switchToTab(index) {
        if (index < 0 || index >= numTabs || index === currentTab) return;

        currentTab = index;
        SoundEngine.switch();
        updateTabUI();
    }

    // Typewriter Effect Variables
    let typeWriterInterval;
    // content is loaded from data-text attribute now
    
    let typeWriterIdx = 0;
    let isTyping = false;

    function startTypewriter() {
        const container = document.getElementById('about-text-content');
        if (!container || container.dataset.typed === 'true') return;
        
        const aboutTextVal = container.getAttribute('data-text') || "";

        // Clear initial content
        container.innerHTML = '';
        typeWriterIdx = 0;
        
        // Create cursor element
        const cursor = document.createElement('span');
        cursor.className = 'cursor';
        cursor.textContent = '|'; // Visually explicit cursor
        container.appendChild(cursor);

        if (isTyping) clearInterval(typeWriterInterval);
        isTyping = true;

        typeWriterInterval = setInterval(() => {
            if (typeWriterIdx < aboutTextVal.length) {
                const char = aboutTextVal.charAt(typeWriterIdx);
                
                if (char === '\\n') { // Check for newline char from string
                    container.insertBefore(document.createElement('br'), cursor);
                } else if (char === '\\n') {
                     container.insertBefore(document.createElement('br'), cursor);
                } else {
                    // Check for actual newline if multiline string
                     if (char.match(/[\r\n]/)) {
                         container.insertBefore(document.createElement('br'), cursor);
                     } else {
                        // Insert text before cursor
                        const textNode = document.createTextNode(char);
                        container.insertBefore(textNode, cursor);
                     }
                }
                
                // Random tiny sound for typing (mechanical keyboard feel)
                if (typeWriterIdx % 3 === 0) SoundEngine.playTone(1200, 'sine', 0.03, 0.02);
                
                typeWriterIdx++;
            } else {
                clearInterval(typeWriterInterval);
                isTyping = false;
                container.dataset.typed = 'true'; // Mark as done so we don't re-type on every tab switch
                
                // Keep cursor blinking at end
            }
        }, FRAME_DURATION); // Speed: 1 frame (~16.74ms)
    }

    function stopTypewriter() {
        if (isTyping) {
            clearInterval(typeWriterInterval);
            isTyping = false;
        }
    }

    function updateActiveLink(index, isWrap = false) {
        const links = document.querySelectorAll('.social-link');
        links.forEach(link => link.classList.remove('active'));
        
        if (links[index]) {
            // Sound blip
            SoundEngine.playTone(440 + (index * 20), 'sine', 0.1, 0.05); // Increasing pitch for selection

            links[index].classList.add('active');
            
            // Ensure element is visible
            if (currentTab === 0) {
                const screen = document.getElementById('main-screen');
                // Check if scroll is needed
                const scrollStyle = isWrap ? 'auto' : getScrollBehavior();
                
                if (index === 0) {
                    // Scroll to top to show header
                    if (screen) screen.scrollTo({ top: 0, behavior: scrollStyle });
                } else if (index === links.length - 1) {
                    // Scroll to bottom to show footer
                    if (screen) screen.scrollTo({ top: screen.scrollHeight, behavior: scrollStyle });
                } else {
                    // Normal scroll into view
                    links[index].scrollIntoView({ behavior: scrollStyle, block: 'nearest' });
                }
            }
            
            currentIndex = index;
        }
    }

    function handleNavigation(direction) {
        // Tab 0: Links navigation
        if (currentTab === 0) {
            const links = document.querySelectorAll('.social-link');
            if (direction === 'up') {
                const isWrap = currentIndex === 0;
                currentIndex = isWrap ? links.length - 1 : currentIndex - 1;
                updateActiveLink(currentIndex, isWrap);
            } else if (direction === 'down') {
                const isWrap = currentIndex === links.length - 1;
                currentIndex = isWrap ? 0 : currentIndex + 1;
                updateActiveLink(currentIndex, isWrap);
            }
        }
        // Tab 1 and 2: About / Help scroll
        else if (currentTab === 1 || currentTab === 2) {
            const screen = document.getElementById('main-screen');
            const scrollAmount = 50;
            
            if (screen) {
                if (direction === 'up') {
                    screen.scrollBy({ top: -scrollAmount, behavior: getScrollBehavior() });
                } else if (direction === 'down') {
                    screen.scrollBy({ top: scrollAmount, behavior: getScrollBehavior() });
                }
            }
        }
    }

    function handleSelection() {
        if (currentTab !== 0) return; // Only select links on tab 0

        const links = document.querySelectorAll('.social-link');
        if (links[currentIndex]) {
            // Trigger click event on the link
            links[currentIndex].click();
            
            // Add a visual 'pressed' effect
            links[currentIndex].classList.add('pressed');
            setTimeout(() => links[currentIndex].classList.remove('pressed'), FRAME_DURATION * 8); // ~134ms

            // Sound effect
            SoundEngine.select();
        }
    }

    function handleThemeSwitch() {
        // Sound effect (12 frames)
        SoundEngine.playTone(200, 'triangle', (FRAME_DURATION * 12) / 1000, 0.05);

        // Cycle to next theme
        currentThemeIndex = (currentThemeIndex + 1) % themes.length;
        
        try {
            localStorage.setItem('gb_theme', themes[currentThemeIndex]);
        } catch(e) {}
        
        // Remove all theme classes
        document.body.classList.remove(...themes);
        
        // Add new theme class
        document.body.classList.add(themes[currentThemeIndex]);
        
        // Visual feedback (flash the screen slightly)
        const screen = document.getElementById('main-screen');
        if (screen) {
            screen.style.opacity = '0.8';
            setTimeout(() => screen.style.opacity = '1', FRAME_DURATION * 6); // ~100ms
        }
    }

    function initNavigation() {
        const keyMap = {
            'ArrowUp': 'up', 'w': 'up', 'W': 'up',
            'ArrowDown': 'down', 's': 'down', 'S': 'down',
            'ArrowLeft': 'left', 'a': 'left', 'A': 'left',
            'ArrowRight': 'right', 'd': 'right', 'D': 'right',
            'Enter': 'btn-a', ' ': 'btn-a', 'z': 'btn-a', 'Z': 'btn-a',
            'x': 'btn-b', 'X': 'btn-b',
            'q': 'btn-select', 'Q': 'btn-select'
        };

        const toggleButtonState = (key, isPressed) => {
            const btnId = keyMap[key];
            if (btnId) {
                const btn = document.getElementById(btnId);
                if (btn) {
                    if (isPressed) btn.classList.add('pressed');
                    else btn.classList.remove('pressed');
                }
            }
        };

        const animatePress = (btn, duration = FRAME_DURATION * 5) => { // ~84ms
            if (!btn) return;
            btn.classList.add('pressed');
            setTimeout(() => btn.classList.remove('pressed'), duration);
        };

        const toggleTurn = () => {
            const btnTurn = document.getElementById('btn-turn');
            const consoleEl = document.querySelector('.console');
            
            if (btnTurn) animatePress(btnTurn);
            SoundEngine.playTone(880, 'sawtooth', (FRAME_DURATION * 6) / 1000, 0.1);

            const performSwitch = () => {
                if (consoleEl) consoleEl.classList.toggle('landscape');
            };

            if (document.startViewTransition) {
                 document.startViewTransition(performSwitch);
            } else {
                 performSwitch();
            }
            
            // Sound feedback for transform
            // 30 frames = 0.5 sec
            setTimeout(() => SoundEngine.playTone(440, 'triangle', (FRAME_DURATION * 30) / 1000, 0.1), FRAME_DURATION * 6);
        };

        document.addEventListener('keyup', (e) => {
            toggleButtonState(e.key, false);
        });

        const clearPressedStates = () => {
            document.querySelectorAll('.pressed').forEach(el => el.classList.remove('pressed'));
        };

        window.addEventListener('blur', clearPressedStates);
        document.addEventListener('visibilitychange', () => {
            if (document.hidden) clearPressedStates();
        });

        document.querySelectorAll('.tab-indicator').forEach((tab, index) => {
            tab.addEventListener('click', () => switchToTab(index));
        });

        // Keyboard navigation
        document.addEventListener('keydown', (e) => {
            const target = e.target;
            const isTypingTarget = target && (
                target.tagName === 'INPUT' ||
                target.tagName === 'TEXTAREA' ||
                target.isContentEditable
            );

            if (isTypingTarget || e.isComposing || e.metaKey) {
                return;
            }

            if (e.key === 'h' || e.key === 'H') {
                if (!e.ctrlKey && !e.altKey && !e.metaKey) {
                    e.preventDefault();
                    switchToTab(2);
                    return;
                }
            }

            if (!e.repeat) toggleButtonState(e.key, true);

            switch(e.key) {
                case 'ArrowUp':
                case 'w':
                case 'W':
                    e.preventDefault();
                    handleNavigation('up');
                    break;
                case 'ArrowDown':
                case 's':
                case 'S':
                    e.preventDefault();
                    handleNavigation('down');
                    break;
                case 'ArrowLeft':
                case 'a':
                case 'A':
                    e.preventDefault();
                    switchTab('left');
                    break;
                case 'ArrowRight':
                case 'd':
                case 'D':
                    e.preventDefault();
                    switchTab('right');
                    break;
                case 'Enter':
                case ' ': // Space - Select / A
                case 'z': // A button often mapped to Z
                case 'Z':
                    e.preventDefault();
                    handleSelection();
                    break;
                case 'x': // B button often mapped to X
                case 'X':
                    if (!e.ctrlKey && !e.altKey && !e.metaKey) {
                        e.preventDefault();
                        handleThemeSwitch();
                    }
                    break;
                case 'q': // Select button mapped to Q
                case 'Q':
                    if (!e.ctrlKey && !e.altKey && !e.metaKey) {
                        e.preventDefault();
                        handleThemeSwitch();
                    }
                    break;
                case 'o': // Turn button mapped to O
                case 'O':
                     if (!e.ctrlKey && !e.altKey && !e.metaKey) {
                        e.preventDefault();
                        toggleTurn();
                    }
                    break;
            }
        });

        // Touch/Click navigation via console buttons
        const btnUp = document.getElementById('up');
        const btnDown = document.getElementById('down');
        const btnLeft = document.getElementById('left');
        const btnRight = document.getElementById('right');
        const btnA = document.getElementById('btn-a');
        const btnB = document.getElementById('btn-b');
        const btnSelect = document.getElementById('btn-select');
        const btnStart = document.getElementById('btn-start');

        if (btnUp) {
            btnUp.addEventListener('click', () => { animatePress(btnUp); handleNavigation('up'); });
            btnUp.addEventListener('touchstart', (e) => { e.preventDefault(); animatePress(btnUp); handleNavigation('up'); });
        }
        
        if (btnDown) {
            btnDown.addEventListener('click', () => { animatePress(btnDown); handleNavigation('down'); });
            btnDown.addEventListener('touchstart', (e) => { e.preventDefault(); animatePress(btnDown); handleNavigation('down'); });
        }

        if (btnLeft) {
            btnLeft.addEventListener('click', () => { animatePress(btnLeft); switchTab('left'); });
            btnLeft.addEventListener('touchstart', (e) => { e.preventDefault(); animatePress(btnLeft); switchTab('left'); });
        }

        if (btnRight) {
            btnRight.addEventListener('click', () => { animatePress(btnRight); switchTab('right'); });
            btnRight.addEventListener('touchstart', (e) => { e.preventDefault(); animatePress(btnRight); switchTab('right'); });
        }

        // A Button: Open Page
        if (btnA) {
            btnA.addEventListener('click', () => { animatePress(btnA); handleSelection(); });
            btnA.addEventListener('touchstart', (e) => { e.preventDefault(); animatePress(btnA); handleSelection(); });
        }
        
        // Start Button: Open Help Tab
        if (btnStart) {
            btnStart.addEventListener('click', () => { animatePress(btnStart); switchToTab(2); });
            btnStart.addEventListener('touchstart', (e) => {
                e.preventDefault();
                animatePress(btnStart);
                switchToTab(2);
            });
        }

        // B Button: Switch Color
        if (btnB) {
            btnB.addEventListener('click', () => {
                 animatePress(btnB);
                 handleThemeSwitch();
            });
            btnB.addEventListener('touchstart', (e) => { 
                e.preventDefault(); 
                animatePress(btnB);
                handleThemeSwitch(); 
            });
        }

        // Select Button: Switch Color
        if (btnSelect) {
            btnSelect.addEventListener('click', () => { animatePress(btnSelect); handleThemeSwitch(); });
            btnSelect.addEventListener('touchstart', (e) => { e.preventDefault(); animatePress(btnSelect); handleThemeSwitch(); });
        }

        // Turn Button (Custom Feature - Switch Layout)
        const btnTurn = document.getElementById('btn-turn');

        if (btnTurn) {
            btnTurn.addEventListener('click', toggleTurn);
            btnTurn.addEventListener('touchstart', (e) => { e.preventDefault(); toggleTurn(); });
        }

        // Allow mouse hover to set active state too
        const linksContainer = document.querySelector('.links');
        if (linksContainer) {
            linksContainer.addEventListener('mouseover', (e) => {
                const link = e.target.closest('.social-link');
                if (link) {
                    const index = parseInt(link.getAttribute('data-index'));
                    if (!isNaN(index)) {
                        // Only update active link if using mouse on Links tab
                        if (currentTab === 0) {
                            updateActiveLink(index);
                        }
                    }
                }
            });
        }

        // Mouse Wheel Support (mapped to D-pad / scrolling)
        let accumulatedDelta = 0;
        let wheelTimeout;
        const LINK_SCROLL_THRESHOLD = 30; // Sensitivity for switching links

        document.addEventListener('wheel', (e) => {
            // Allow zoom
            if (e.ctrlKey) return; 
            
            e.preventDefault(); // Disable default page scroll

            // Detect current context
            if (currentTab === 1 || currentTab === 2) {
                // About/Help tabs: smooth scroll content
                const screen = document.getElementById('main-screen');
                if (screen) {
                    screen.scrollBy({ top: e.deltaY, behavior: 'auto' });
                }
            } else {
                // Links tab: debounced navigation
                accumulatedDelta += e.deltaY;
                
                if (Math.abs(accumulatedDelta) > LINK_SCROLL_THRESHOLD) {
                    if (accumulatedDelta > 0) handleNavigation('down');
                    else handleNavigation('up');
                    
                    accumulatedDelta = 0; // Reset after action
                }
                
                // Reset accumulator if idle
                clearTimeout(wheelTimeout);
                wheelTimeout = setTimeout(() => { accumulatedDelta = 0; }, 100);
            }
        }, { passive: false });
    }

    function initExtras() {
        // 1. Start Screen Overlay
        const overlay = document.getElementById('start-overlay');
        
        // Helper to request audio context (if added later)
        const unlockAudio = () => {
            SoundEngine.init();
        };

        if (overlay) {
            let dismissed = false;
            const dismissOverlay = (e) => {
                 if (dismissed) return;
                 
                 // Prevent input from triggering game actions just this once
                 e.preventDefault();
                 e.stopImmediatePropagation();
                 
                 dismissed = true;
                 overlay.style.display = 'none';
                 
                 unlockAudio();
                 
                 document.removeEventListener('keydown', dismissOverlay);
                 document.removeEventListener('click', dismissOverlay);
                 document.removeEventListener('touchstart', dismissOverlay);
            };
            
            document.addEventListener('keydown', dismissOverlay);
            document.addEventListener('click', dismissOverlay);
            document.addEventListener('touchstart', dismissOverlay);
        }

        // 2. Konami Code (Up, Up, Down, Down, Left, Right, Left, Right, B, A)
        const konamiCode = [
            'ArrowUp', 'ArrowUp', 
            'ArrowDown', 'ArrowDown', 
            'ArrowLeft', 'ArrowRight', 
            'ArrowLeft', 'ArrowRight', 
            'x', 'z' // Assuming B=x and A=z/enter. The button mapping is x->btn-b, z->btn-a.
        ];
        let konamiIndex = 0;

        document.addEventListener('keydown', (e) => {
            // Check key - allow 'b'/'a' explicitly or mapped keys
            const key = e.key.toLowerCase();
            const expected = konamiCode[konamiIndex].toLowerCase();
            
            let match = false;
            if (key === expected) match = true;
            
            // Handle explicit B/A regardless of mapping
            if (konamiIndex === 8) { // Expecting B
                 if (key === 'b') match = true;
            }
            if (konamiIndex === 9) { // Expecting A
                 if (key === 'a' || key === 'enter') match = true;
            }

            if (match) {
                konamiIndex++;
                if (konamiIndex === konamiCode.length) {
                    // Activate Easter Egg
                    activateMatrixMode();
                    konamiIndex = 0;
                }
            } else {
                konamiIndex = 0; 
            }

            // Mute Toggle (M key)
            if (key === 'm') {
                SoundEngine.toggleMute();
            }
        });

        // 3. Battery Indicator (Keep this)
        const level = document.getElementById('battery-level');
        if (level && 'getBattery' in navigator) {
            navigator.getBattery().then(function(battery) {
                const updateBattery = () => {
                    level.style.width = `${battery.level * 100}%`;
                    if (battery.charging) {
                        level.classList.add('charging');
                        // Use theme variable for color consistency
                        level.style.backgroundColor = 'var(--screen-text)'; 
                    } else {
                        level.classList.remove('charging');
                        if (battery.level < 0.2) level.style.backgroundColor = '#ff0055'; // Red
                        else if (battery.level < 0.5) level.style.backgroundColor = '#ffff00'; // Yellow
                        else level.style.backgroundColor = 'var(--screen-text)'; // Theme Default
                    }
                };
                updateBattery();
                battery.addEventListener('levelchange', updateBattery);
                battery.addEventListener('chargingchange', updateBattery);
            }).catch(() => {
                level.style.width = '85%';
                level.style.backgroundColor = 'var(--screen-text)';
            });
        } else if (level) {
            level.style.width = '85%';
            level.style.backgroundColor = 'var(--screen-text)';
        }

    }

    function activateMatrixMode() {
        const screen = document.getElementById('main-screen');
        if (screen) {
            screen.style.filter = "invert(1) hue-rotate(180deg)";
            setTimeout(() => {
                screen.style.filter = "";
            }, FRAME_DURATION * 300); // ~5000ms
        }
    }

    function initPowerSwitch() {
        const switchTrack = document.getElementById('power-switch');
        const consoleBody = document.querySelector('.console');
        const screenDisplay = document.querySelector('.screen-display');
        
        let isPoweredOn = true;

        if (!switchTrack || !consoleBody) return;

        const togglePower = () => {
            isPoweredOn = !isPoweredOn;
            
            switchTrack.setAttribute('aria-checked', isPoweredOn);
            const bootScreen = document.getElementById('boot-screen');
            const topHud = document.getElementById('top-hud');
            
            if (isPoweredOn) {
                // Power ON
                consoleBody.classList.remove('console-off');
                SoundEngine.playTone(100, 'sawtooth', (FRAME_DURATION * 6) / 1000); // Click sound (6 frames)
                
                // Show boot screen and process gram
                if (bootScreen) bootScreen.classList.add('active');
                if (topHud) topHud.style.opacity = '0';
                
                // Delay boot chime and UI reveal
                setTimeout(() => {
                    if (!isPoweredOn) return; // Abort if switched off during boot
                    SoundEngine.playTone(600, 'sine', (FRAME_DURATION * 24) / 1000); // Boot chime (24 frames)
                    if (bootScreen) bootScreen.classList.remove('active');
                    if (topHud) topHud.style.opacity = '1';
                }, 3500); // 3-5 sec boot as requested
                
                // Re-trigger screen animation
                if (screenDisplay) {
                    screenDisplay.style.animation = 'none';
                    screenDisplay.offsetHeight; /* trigger reflow */
                    screenDisplay.style.animation = 'turnOn 0.3s ease-out';
                }
                
                document.title = "Hey!";
            } else {
                // Power OFF
                SoundEngine.playTone(100, 'sawtooth', (FRAME_DURATION * 6) / 1000); // Click sound
                SoundEngine.playTone(50, 'square', (FRAME_DURATION * 18) / 1000, 0.2); // Power down buzz (18 frames)
                
                consoleBody.classList.add('console-off');
                document.title = "OFF";
                
                if (bootScreen) bootScreen.classList.remove('active');
                if (topHud) topHud.style.opacity = '1';

                // Reset System State
                setTimeout(() => {
                    // Reset to Links Tab (Tab 0)
                    if (currentTab !== 0) {
                        switchToTab(0);
                    }
                    
                    // Reset Link Selection to first item
                    currentIndex = 0;
                    updateActiveLink(0);
                    
                    // Reset Scroll Position
                    if (screenDisplay) {
                        screenDisplay.scrollTo({ top: 0, behavior: 'auto' });
                    }
                }, 100); // Small delay to hide reset behind the off-screen animation
            }
        };

        switchTrack.addEventListener('click', togglePower);
        
        // Keyboard accessibility for switch
        switchTrack.addEventListener('keydown', (e) => {
            if (e.key === 'Enter' || e.key === ' ') {
                e.preventDefault();
                togglePower();
            }
        });

        // Global keyboard shortcut (P) for power toggle
        document.addEventListener('keydown', (e) => {
            // Avoid triggering when typing in inputs/textareas
            const target = e.target;
            const isTypingTarget = target && (
                target.tagName === 'INPUT' ||
                target.tagName === 'TEXTAREA' ||
                target.isContentEditable
            );

            if (isTypingTarget || e.metaKey || e.ctrlKey || e.altKey) return;

            if (e.key === 'p' || e.key === 'P') {
                e.preventDefault();
                togglePower();
            }
        });
    }

    // Initialize when DOM is ready
    document.addEventListener('DOMContentLoaded', () => {
        // 1. Init Extras (Overlay, Battery, Konami)
        initExtras();
        initPowerSwitch();
        updateMuteIcon();

        // Apply initial random theme
        document.body.classList.add(themes[currentThemeIndex]);
        
        // Title & Favicon change on visibility change
        const originalTitle = document.title;
        const setFavicon = (emoji) => {
            const link = document.querySelector("link[rel*='icon']") || document.createElement('link');
            link.type = 'image/svg+xml';
            link.rel = 'icon';
            link.href = `data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>${emoji}</text></svg>`;
            document.getElementsByTagName('head')[0].appendChild(link);
        };

        document.addEventListener('visibilitychange', () => {
            if (document.hidden) {
                document.title = "Where'd you go?";
                setFavicon('🤔');
            } else {
                document.title = originalTitle;
                setFavicon('👋');
            }
        });

        // renderSocialLinks(); // Removed as we render via Astro
        initNavigation();
        updateTabUI();
        initHudClock();
    });

</script>

<style is:global>
/* --- Console Body --- */
@keyframes whipIn {
    0% { transform: translateX(100vw) rotate(5deg) scale(0.9); opacity: 0; }
    60% { transform: translateX(-15px) rotate(-1deg) scale(1.02); opacity: 1; }
    80% { transform: translateX(5px) rotate(0.5deg) scale(0.99); }
    100% { transform: translateX(0) rotate(0deg) scale(1); opacity: 1; }
}

@keyframes consoleFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-12px); }
}

.console {
    position: relative;
    background: linear-gradient(135deg, var(--console-body), var(--console-shadow));
    backdrop-filter: blur(10px);
    box-shadow: 
        inset 3px 3px 6px rgba(255,255,255,0.3), /* Enhanced top highlight */
        inset -5px -5px 15px rgba(0,0,0,0.5),
        0 0 15px rgba(120, 60, 220, 0.3), /* Ambient glow to define edges */
        25px 25px 60px rgba(0,0,0,0.7); /* Deep shadow for better separation */
    display: flex;
    justify-content: center;
    align-items: center;
    transform-style: preserve-3d;
    border: 2px solid rgba(255,255,255,0.25); /* Broader, clearer border */
    /* Entrance + Idle Float Animation */
    animation: 
        whipIn 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards,
        consoleFloat 6s ease-in-out infinite 0.8s;
    
    /* Default Portrait Dimensions */
    width: 420px;
    height: 720px;
    border-radius: 40px; /* Constant border radius */
    padding: 40px 30px;
    
    /* Ensure it is centered in the viewport */
    margin: 0 auto;
}

.console.landscape {
    width: 860px; /* Landscape GBA Style */
    height: 400px;
    /* border-radius removed to keep it constant (inherits 40px) */
    padding: 20px;
}

/* Layout Containers */
/* Portrait Grid Layout */
.console-body {
    width: 100%;
    height: 100%;
    position: relative;
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: auto 1fr auto;
    grid-template-areas: 
        "screen screen"
        "left right"
        "meta speaker";
    gap: 10px;
}

.console.landscape .console-body {
    display: grid;
    grid-template-columns: 180px 1fr 180px;
    grid-template-rows: 1fr auto;
    grid-template-areas: 
        "left screen right"
        "meta screen speaker";
    align-items: center;
    gap: 15px;
    width: 100%; /* Ensure full width usage */
}

/* View Transition Names */
.console { view-transition-name: console-shell; }
.screen-lens { view-transition-name: console-screen; }
/* Animate the inner components for better rotation/transform interpolation */
.dpad { view-transition-name: console-dpad; } 
.action-buttons { view-transition-name: none; } /* Disable container transition to let children fly */
.group-a { view-transition-name: btn-group-a; }
.group-b { view-transition-name: btn-group-b; }
.meta-controls { view-transition-name: none; grid-area: meta; }
/* .pill-btn { view-transition-name: console-pill; } */ /* Removed generic selector */
.speaker { view-transition-name: none; } /* Disabled container morph */
/* Power Switch uses its own transition logic declared below */


/* Custom Transition Physics */
::view-transition-group(root),
::view-transition-group(console-shell),
::view-transition-group(console-screen),
::view-transition-group(console-dpad),
/* ::view-transition-group(console-buttons), */
::view-transition-group(btn-group-a),
::view-transition-group(btn-group-b),
/* ::view-transition-group(console-pill), */
::view-transition-group(btn-select),
::view-transition-group(btn-start),
::view-transition-group(label-select),
::view-transition-group(label-start),
/* ::view-transition-group(console-speaker), */
::view-transition-group(s1), ::view-transition-group(s2), ::view-transition-group(s3), 
::view-transition-group(s4), ::view-transition-group(s5), ::view-transition-group(s6), 
::view-transition-group(console-power-switch) {
    /* "Nintendo Snappy" - Fast attack, solid impact, no wobble */
    animation-timing-function: cubic-bezier(0.12, 1, 0.14, 1);
    animation-duration: 0.5s;
}

/* Add GBA Boot Flash to Screen Morph */
@keyframes gbaFlash {
    0% { filter: brightness(1); }
    40% { filter: brightness(1.3) contrast(0.9); }
    100% { filter: brightness(1); }
}

::view-transition-new(console-screen) {
    animation: gbaFlash 0.5s cubic-bezier(0.12, 1, 0.14, 1);
    height: 100%;
    width: 100%;
}

/* Ensure children and SHELL morph smoothly */
::view-transition-old(console-shell),
::view-transition-new(console-shell),
::view-transition-old(console-screen),
::view-transition-new(console-screen),
::view-transition-old(console-dpad),
::view-transition-new(console-dpad),
/* ::view-transition-old(console-buttons),
::view-transition-new(console-buttons), */
::view-transition-old(btn-group-a),
::view-transition-new(btn-group-a),
::view-transition-old(btn-group-b),
::view-transition-new(btn-group-b),
::view-transition-old(btn-select),
::view-transition-new(btn-select),
::view-transition-old(console-power-switch),
::view-transition-new(console-power-switch),
::view-transition-old(btn-start),
::view-transition-new(btn-start),
::view-transition-old(label-select),
::view-transition-new(label-select),
::view-transition-old(label-start),
::view-transition-new(label-start),
::view-transition-old(s1), ::view-transition-new(s1),
::view-transition-old(s2), ::view-transition-new(s2),
::view-transition-old(s3), ::view-transition-new(s3),
::view-transition-old(s4), ::view-transition-new(s4),
::view-transition-old(s5), ::view-transition-new(s5),
::view-transition-old(s6), ::view-transition-new(s6) {
    height: 100%;
    width: 100%;
}

/* Prevent cross-fade ghosting on console body */
::view-transition-old(console-shell) {
    display: none;
}
::view-transition-new(console-shell) {
    animation: none;
}


.left-controls, .right-controls {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    position: relative;
}

.left-controls {
    grid-area: left;
    justify-content: flex-start;
    padding-top: 20px;
}

/* Landscape D-Pad Animation: "Lock and Load" Spin/Scale */
.console.landscape .left-controls {
    align-items: center;
    padding-right: 10px;
    padding-top: 0;
}

.console.landscape .dpad {
    transform: scale(1.1) rotate(360deg); /* 10% bigger + Full Spin */
    box-shadow: 0 0 15px rgba(0,0,0,0.5); /* Deepen shadow */
    transition: transform 0.6s cubic-bezier(0.4, 0.0, 0.2, 1);
}

/* Counter-rotate buttons so symbols stay upright if they had any */
/* (Currently they don't, but shadows do) */

.right-controls {
    grid-area: right;
    justify-content: flex-start;
    padding-top: 40px; /* Buttons typically lower than Dpad on DMG */
}

.console.landscape .left-controls, 
.console.landscape .right-controls {
    flex: 0 0 180px;
    height: 100%;
    padding: 0;
    justify-content: center;
}

.console.landscape .left-controls {
    align-items: center;
    padding-right: 10px;
}

.console.landscape .right-controls {
    align-items: center;
    padding-left: 10px;
}


/* --- Power Switch --- */
.power-switch-container {
    position: absolute;
    top: 60px; 
    left: -12px;
    z-index: -1;
    transform-style: preserve-3d;
    cursor: pointer;
    padding: 10px 20px 10px 0;
    view-transition-name: console-power-switch;
}

.console.landscape .power-switch-container {
    left: 178px; /* Move closer to screen (was 170px) */
    top: 45px;
    z-index: 5; 
}

/* Landscape Power Switch Sizing */
.console.landscape .switch-track {
    width: 18px; /* Narrower width */
    height: 40px; /* Slightly shorter */
}
.console.landscape .switch-handle {
    width: 16px;
    height: 14px; /* Adjust handle size match */
}
.console.landscape .switch-track[aria-checked="false"] .switch-handle {
    top: 24px; /* Adjust travel distance for shorter track */
}

/* Ensure Power Switch is below Screen Lens during transition */
::view-transition-group(console-power-switch) {
    z-index: 5;
}

::view-transition-group(console-screen) {
    z-index: 20; 
}

/* Remove separate timing declaration to avoid duplication since we added it to the main group block */
.switch-track {
    width: 25px;
    height: 50px;
    background: #111;
    border-radius: 5px 0 0 5px;
    position: relative;
    box-shadow: 
        inset 2px 0 5px rgba(255,255,255,0.1),
        -2px 2px 5px rgba(0,0,0,0.5);
    border: 1px solid #222;
    border-right: none;
    overflow: hidden;
}

.switch-handle {
    width: 24px;
    height: 18px;
    background: #444;
    background-image: repeating-linear-gradient(0deg, transparent 0, transparent 2px, rgba(0,0,0,0.5) 2px, rgba(0,0,0,0.5) 3px);
    position: absolute;
    left: 1px;
    border-radius: 3px 0 0 3px;
    box-shadow: 
        inset 1px 0 1px rgba(255,255,255,0.3),
        0 2px 4px rgba(0,0,0,0.8);
    transition: top 0.2s cubic-bezier(0.4, 0.0, 0.2, 1);
    z-index: 2;
}

.switch-track[aria-checked="false"] .switch-handle {
    top: 30px;
    background-color: #333;
}

.switch-track[aria-checked="true"] .switch-handle {
    top: 2px;
    background-color: #555;
    box-shadow: 0 0 10px rgba(0, 255, 0, 0.2);
}

/* --- Powered OFF State --- */
.console-off .screen-display {
    background: #050505 !important;
    background-image: none !important;
    color: #111 !important;
    text-shadow: none !important;
    box-shadow: inset 0 0 50px #000 !important;
    overflow: hidden;
}

.console-off .screen-display * {
    opacity: 0 !important; /* Hide content */
    transition: opacity 0.1s;
}

.console-off .power-indicator .led {
    background: #330000;
    box-shadow: none;
    animation: none;
    opacity: 0.3;
}

.console-off .dpad-btn, 
.console-off .action-buttons button {
    pointer-events: none; /* Disable input */
}


/* --- Screen Lens area --- */
.screen-lens {
    background: var(--lens-bg);
    grid-area: screen;
    width: 100%;
    align-self: flex-start;
    border-radius: 15px 15px 45px 15px; /* Standardize specific curve */
    padding: 30px 40px 15px 40px;
    position: relative;
    box-shadow: 0 5px 15px rgba(0,0,0,0.5);
    margin-bottom: 20px;
    border: 1px solid #333;
    display: flex;
    flex-direction: column;
    justify-content: center;
}

/* System Controls (Select/Start) - PlayStation Style Top Layout (Removed - Moved to console body for dynamic placement) */
.console.landscape .screen-lens {
    flex: 1; /* Fill center space */
    align-self: stretch;
    border-radius: 15px 15px 45px 15px; /* Keep consistent curve to avoid reshaping distortion */
    margin: 10px 0;
}

/* Screen Rainbow Stripes */
.screen-lens::before {
    content: '';
    position: absolute;
    top: 25px;
    left: 40px;
    right: 40px;
    height: 3px;
    background: linear-gradient(90deg, #ff0055, #a210bc, #00ccff);
    box-shadow: 0 0 5px rgba(255,255,255,0.2);
}

.logo {
    font-family: sans-serif;
    color: #ccc;
    font-style: italic;
    font-weight: bold;
    font-size: 14px;
    text-align: center;
    margin-top: 5px;
    width: 100%;
}
.logo .advance {
    font-family: 'Press Start 2P', cursive;
    color: #ff0055; /* Match accent */
    font-style: normal;
    font-size: 10px;
    margin-left: 5px;
    letter-spacing: 1px;
}

/* --- The Battery LED --- */
.power-indicator {
    position: absolute;
    top: 50%;
    left: 15px;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
}

.power-indicator .led {
    width: 8px;
    height: 8px;
    background: #0aff00; /* Green for GBA vibe */
    border-radius: 50%;
    box-shadow: 0 0 6px #0aff00;
    animation: pulse 3s infinite;
}

@keyframes pulse { 0% { opacity: 0.6; } 50% { opacity: 1; } 100% { opacity: 0.6; } }

.screen-display {
    background: var(--screen-bg);
    width: 100%;
    height: 230px;
    box-shadow: inset 0 0 20px rgba(0,0,0,0.8);
    overflow: hidden; /* Only programmatic scroll */
    padding: 15px;
    font-family: 'VT323', monospace;
    font-size: 1.4rem;
    color: var(--screen-text);
    position: relative;
    border-radius: 4px;
    
    /* Modern Scanline */
    background-image: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06));
    background-size: 100% 2px, 3px 100%;
}

/* --- Boot Screen / Process Gram --- */
.boot-screen {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--screen-bg);
    z-index: 10;
    display: flex;
    justify-content: center;
    align-items: center;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.2s ease, visibility 0.2s ease;
}

.boot-screen.active {
    opacity: 1;
    visibility: visible;
}

.progress-container {
    width: 60%;
    height: 16px; /* Slightly taller for distinct blocks */
    border: 3px solid var(--screen-text); /* Thicker border for retro feel */
    border-radius: 4px; /* Less rounded, more blocky */
    padding: 2px;
    box-shadow: 0 0 5px var(--screen-text);
    background: rgba(0, 0, 0, 0.4); /* Darker background */
    display: flex;
    align-items: center;
}

.progress-bar {
    width: 100%;
    height: 100%;
    background: var(--screen-text);
    border-radius: 2px;
}

.boot-screen.active .progress-bar {
    /* jump-none means the animation reaches 100% at the end frame naturally across 5 blocks */
    animation: fillProgressBlocks 3.5s steps(5, jump-none) forwards;
}

@keyframes fillProgressBlocks {
    0% { clip-path: inset(0 100% 0 0); }
    100% { clip-path: inset(0 0% 0 0); }
}

/* Glass Reflection */
.screen-lens::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: inherit; /* Copies the 15px 15px 45px 15px inherited from screen-lens */
    background: linear-gradient(110deg, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0.03) 45%, transparent 45.1%);
    pointer-events: none;
    z-index: 100;
}

/* --- Top HUD --- */
.top-hud {
    position: absolute;
    top: 12px;
    left: 15px;
    right: 60px; /* Leave room for battery on the right */
    display: flex;
    align-items: center;
    gap: 14px;
    font-size: 13px;
    line-height: 1;
    color: var(--screen-text);
    z-index: 3;
    pointer-events: none;
}

.hud-item {
    display: inline-flex;
    align-items: center;
    white-space: nowrap;
    text-shadow: 0 0 4px rgba(10, 255, 194, 0.35);
}

/* --- Branding Text on Lens --- */
.brand-text {
    text-align: center;
    color: #666;
    font-style: italic;
    font-size: 14px;
    margin-top: 12px;
    font-family: sans-serif;
    font-weight: bold;
    letter-spacing: 1px;
}

.brand-text span {
    color: #ccc;
    font-weight: 800;
}

.brand-text .color-text {
    font-family: 'Press Start 2P', cursive;
    background: linear-gradient(90deg, #ff0055, #00ccff);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent; 
    font-size: 16px;
    position: relative;
    top: 1px;
}

/* --- Profile Inside Screen --- */
.profile {
    text-align: center;
    margin-bottom: 15px;
    border-bottom: 1px solid rgba(10, 255, 194, 0.3);
    padding-bottom: 10px;
}

.avatar-wrapper {
    width: 80px;
    height: 80px;
    margin: 0 auto 10px;
    border: 2px solid var(--screen-text);
    box-shadow: 0 0 10px var(--screen-text);
    border-radius: 5px;
    overflow: hidden;
}

.avatar {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: sepia(1) hue-rotate(130deg) saturate(2) contrast(1.2);
    image-rendering: pixelated;
}

h1 {
    font-size: 24px;
    margin: 5px 0;
    text-transform: uppercase;
    text-shadow: 2px 2px 0 var(--screen-dim);
}

.tagline {
    font-size: 16px;
    margin: 0;
    opacity: 0.8;
    color: var(--screen-text);
    text-shadow: 1px 1px 0 var(--screen-dim);
}

/* --- Tabs --- */
.tabs-header {
    display: flex;
    justify-content: center;
    gap: 30px;
    margin-bottom: 20px;
    border-bottom: 2px solid var(--screen-dim);
    padding-bottom: 5px;
    margin-top: 15px; /* Reduced from 40px */
}

.tab-indicator {
    appearance: none;
    border: 0;
    background: transparent;
    font-size: 16px;
    cursor: pointer;
    opacity: 0.5;
    transition: opacity 0.2s, text-shadow 0.2s;
    color: var(--screen-text);
    text-transform: uppercase;
    font-family: 'VT323', monospace;
}

.tab-indicator:hover {
    opacity: 0.8;
}

.tab-indicator.active {
    opacity: 1;
    font-weight: bold;
    text-shadow: 0 0 5px var(--screen-text);
    border-bottom: 2px solid var(--screen-text);
    margin-bottom: -7px; /* Align with border-bottom */
}

.tab-content {
    display: none;
    animation: fadeIn 0.3s ease-out;
}

.tab-content.active {
    display: block;
}

@keyframes fadeIn {
    from { opacity: 0; transform: translateY(5px); }
    to { opacity: 1; transform: translateY(0); }
}

.about-text {
    font-family: 'VT323', monospace;
    font-size: 20px;
    line-height: 1.4;
    color: var(--screen-text);
    text-shadow: 0 0 2px var(--screen-text);
    padding: 10px;
    background: rgba(0, 0, 0, 0.2);
    border: 1px dotted var(--screen-dim);
    min-height: 100px;
}

.help-panel {
    padding: 10px;
    border: 1px dotted var(--screen-dim);
    background: rgba(0, 0, 0, 0.25);
    font-size: 18px;
    color: var(--screen-text);
}

.help-panel h3 {
    margin: 0 0 10px;
    font-size: 20px;
    color: var(--screen-text);
    text-shadow: 0 0 4px var(--screen-dim);
    border-bottom: 2px solid var(--screen-dim);
    padding-bottom: 4px;
}

.help-panel ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 6px;
}

.help-panel li {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    border-bottom: 1px dashed var(--screen-dim);
    padding-bottom: 2px;
}

.help-panel li span:first-child {
    font-weight: bold;
    color: var(--screen-text);
}

.secret-hint {
    margin: 10px 0 0;
    color: var(--screen-dim);
    background: var(--screen-text);
    padding: 4px;
    text-align: center;
    font-weight: bold;
    letter-spacing: 1px;
    font-size: 14px;
    white-space: nowrap;
}

/* --- Typewriter Cursor --- */
.cursor {
    display: inline-block;
    width: 10px;
    height: 1em;
    background-color: var(--screen-text);
    animation: blink 1s infinite;
    vertical-align: bottom;
}

/* --- Battery Indicator --- */
.battery {
    position: absolute;
    top: 15px;
    right: 20px;
    width: 30px;
    height: 14px;
    border: 2px solid var(--screen-dim);
    border-radius: 2px;
    padding: 1px;
    display: flex;
    align-items: center;
}

.battery::after {
    content: '';
    position: absolute;
    right: -4px;
    top: 3px;
    width: 2px;
    height: 4px;
    background: var(--screen-dim);
}

.battery-level {
    height: 100%;
    width: 0%; /* JS will update */
    background: var(--screen-dim);
    transition: width 0.5s, background-color 0.5s;
}

.battery-level.charging {
    animation: charge 2s steps(5) infinite;
    transition: none;
}

@keyframes charge {
    0% { width: 0%; }
    100% { width: 100%; }
}

/* --- Start Screen Overlay --- */
#start-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--screen-bg);
    /* inner shadow to match screen */
    box-shadow: inset 0 0 20px rgba(0,0,0,0.8);
    z-index: 100;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    color: var(--screen-text);
    font-size: 20px;
}

.blink {
    animation: blink 1s infinite;
}

@keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
}

/* --- CRT Turn On Effect --- */
@keyframes turnOn {
    0% { transform: scale(1, 0.002); opacity: 0; }
    30% { transform: scale(1, 0.002); opacity: 1; }
    50% { transform: scale(1, 0.002); opacity: 1; }
    70% { transform: scale(1, 1); opacity: 1; }
    100% { transform: scale(1, 1); opacity: 1; }
}

.screen-display {
    animation: turnOn 0.3s ease-out;
    /* Maintain other styles */
    overflow-y: auto;
    scrollbar-width: none; /* Firefox */
    -ms-overflow-style: none; /* IE 10+ */
}

/* Hide scrollbar for Chrome, Safari and Opera */
.screen-display::-webkit-scrollbar {
    display: none;
}

/* --- Links Inside Screen --- */
.links {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.links a {
    display: flex;
    align-items: center;
    color: var(--screen-text);
    text-decoration: none;
    padding: 8px 10px;
    border: 1px solid transparent;
    transition: all 0.1s;
    font-size: 20px;
}

.links a:hover, .links a.active {
    background: var(--screen-text);
    color: var(--screen-bg);
    box-shadow: 0 0 10px var(--screen-text);
    font-weight: bold;
}

.tab-indicator:focus-visible,
.links a:focus-visible,
.dpad-btn:focus-visible,
.action-buttons button:focus-visible,
.pill-btn:focus-visible {
    outline: 2px solid var(--screen-text);
    outline-offset: 2px;
}

.links a svg {
    width: 20px; 
    height: 20px;
    margin-right: 12px;
    fill: currentColor;
}


/* --- Controls Section --- */
/* (Old .controls wrapper removed) */

/* Functional D-Pad */
.dpad-area {
    width: 140px;
    height: 140px;
    position: relative;
    /* margin-bottom handled by flex gap */
}

.dpad {
    width: 130px;
    height: 130px;
    position: relative;
    border-radius: 50%;
    /* Recessed Socket */
    background: rgba(0,0,0,0.05);
    box-shadow: 
        inset 2px 2px 8px rgba(0,0,0,0.6), 
        inset -1px -1px 3px rgba(255,255,255,0.1),
        0 1px 1px rgba(255,255,255,0.2);
    transition: transform 0.6s cubic-bezier(0.4, 0.0, 0.2, 1);
}

.dpad-btn {
    position: absolute;
    background-color: var(--btn-dpad);
    border: none;
    cursor: pointer;
    /* Clean matte finish instead of gradient dots */
    background-image: linear-gradient(135deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 40%, rgba(0,0,0,0.2) 100%);
    transition: all 0.08s cubic-bezier(0.3, 0.8, 0.3, 1);
    outline: none;
    z-index: 2;
    display: flex;
    justify-content: center;
    align-items: center;
}

/* Subtle engraved arrows instead of nothing */
.dpad-btn::after {
    content: '';
    display: block;
    width: 0;
    height: 0;
    border-style: solid;
    opacity: 0.15;
}

.dpad-up {
    top: 12px;
    left: 45px;
    width: 40px;
    height: 40px;
    border-radius: 4px 4px 0 0;
    box-shadow: 
        inset 0 1.5px 2px rgba(255,255,255,0.2), 
        inset 1px 0 2px rgba(255,255,255,0.1), 
        inset -1px 0 2px rgba(0,0,0,0.2), 
        0 4px 0 rgba(0,0,0,0.7), 
        0 6px 10px rgba(0,0,0,0.5); 
}
.dpad-up::after { border-width: 0 6px 7px 6px; border-color: transparent transparent #000 transparent; margin-bottom: 8px;}

.dpad-down {
    bottom: 12px;
    left: 45px;
    width: 40px;
    height: 40px;
    border-radius: 0 0 4px 4px;
    box-shadow: 
        inset 1px 0 2px rgba(255,255,255,0.1), 
        inset -1px 0 2px rgba(0,0,0,0.2), 
        inset 0 -2px 4px rgba(0,0,0,0.5), 
        0 4px 0 rgba(0,0,0,0.7), 
        0 6px 10px rgba(0,0,0,0.5); 
}
.dpad-down::after { border-width: 7px 6px 0 6px; border-color: #000 transparent transparent transparent; margin-top: 8px; }

.dpad-left {
    top: 45px;
    left: 12px;
    width: 40px;
    height: 40px;
    border-radius: 4px 0 0 4px;
    box-shadow: 
        inset 1.5px 0 2px rgba(255,255,255,0.2), 
        inset 0 1px 2px rgba(255,255,255,0.1), 
        inset 0 -1px 2px rgba(0,0,0,0.2), 
        0 4px 0 rgba(0,0,0,0.7), 
        0 6px 10px rgba(0,0,0,0.5); 
}
.dpad-left::after { border-width: 6px 7px 6px 0; border-color: transparent #000 transparent transparent; margin-right: 8px; }

.dpad-right {
    top: 45px;
    right: 12px;
    width: 40px;
    height: 40px;
    border-radius: 0 4px 4px 0;
    box-shadow: 
        inset 0 1px 2px rgba(255,255,255,0.1), 
        inset 0 -1px 2px rgba(0,0,0,0.2), 
        inset -2px 0 4px rgba(0,0,0,0.5), 
        0 4px 0 rgba(0,0,0,0.7), 
        0 6px 10px rgba(0,0,0,0.5); 
}
.dpad-right::after { border-width: 6px 0 6px 7px; border-color: transparent transparent transparent #000; margin-left: 8px; }

.dpad-center {
    position: absolute;
    top: 45px;
    left: 45px;
    width: 40px;
    height: 40px;
    background-color: var(--btn-dpad);
    background-image: linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0) 50%, rgba(0,0,0,0.1) 100%);
    z-index: 3;
    /* Fill gaps between directional arms and add center recess */
    box-shadow: 
        -4px 0 0 var(--btn-dpad), 
        4px 0 0 var(--btn-dpad), 
        0 -4px 0 var(--btn-dpad), 
        0 4px 0 var(--btn-dpad),
        0 4px 0 rgba(0,0,0,0.7), 
        0 6px 10px rgba(0,0,0,0.5); 
}

/* Subtle thumb divot in center */
.dpad-center::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 16px;
    height: 16px;
    border-radius: 50%;
    background: rgba(0,0,0,0.1);
    box-shadow: inset 1px 1px 3px rgba(0,0,0,0.4), 0 1px 1px rgba(255,255,255,0.05);
}

.dpad-btn:active,
.dpad-btn.pressed {
    /* Need to keep background colors but remove highlight */
    background-image: linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(0,0,0,0.1) 40%, rgba(0,0,0,0.3) 100%);
}

.dpad-up:active, .dpad-up.pressed {
    transform: translateY(4px);
    box-shadow: inset 0 2px 4px rgba(0,0,0,0.5), inset 1px 0 2px rgba(0,0,0,0.2), inset -1px 0 2px rgba(0,0,0,0.2), 0 0px 0 rgba(0,0,0,0.6), 0 2px 4px rgba(0,0,0,0.5);
}
.dpad-down:active, .dpad-down.pressed {
    transform: translateY(4px);
    box-shadow: inset 0 2px 4px rgba(0,0,0,0.5), inset 1px 0 2px rgba(0,0,0,0.2), inset -1px 0 2px rgba(0,0,0,0.2), 0 0px 0 rgba(0,0,0,0.6), 0 2px 4px rgba(0,0,0,0.5);
}
.dpad-left:active, .dpad-left.pressed {
    transform: translateY(4px);
    box-shadow: inset 0 2px 4px rgba(0,0,0,0.5), inset 1.5px 0 2px rgba(0,0,0,0.2), inset 0 -1px 2px rgba(0,0,0,0.2), 0 0px 0 rgba(0,0,0,0.6), 0 2px 4px rgba(0,0,0,0.5);
}
.dpad-right:active, .dpad-right.pressed {
    transform: translateY(4px);
    box-shadow: inset 0 2px 4px rgba(0,0,0,0.5), inset -2px 0 4px rgba(0,0,0,0.2), inset 0 -1px 2px rgba(0,0,0,0.2), 0 0px 0 rgba(0,0,0,0.6), 0 2px 4px rgba(0,0,0,0.5);
}

/* Functional Buttons A & B */
.action-buttons {
    display: flex;
    gap: 20px;
    transform: rotate(25deg);
    margin-bottom: 30px;
    padding-left: 20px;
    margin-top: 15px; /* Adjusted to balance against D-Pad */
}

.console.landscape .action-buttons {
    transform: rotate(-15deg);
    margin-bottom: 20px;
    padding-left: 0;
    gap: 15px;
}

.btn-group {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    position: relative;
    z-index: 1;
}

.btn-group::before {
    content: '';
    position: absolute;
    top: -5px;
    width: 66px;
    height: 66px;
    border-radius: 50%;
    background: rgba(0,0,0,0.05);
    box-shadow: 
        inset 2px 2px 5px rgba(0,0,0,0.6), 
        inset -1px -1px 3px rgba(255,255,255,0.1),
        0 1px 1px rgba(255,255,255,0.2); 
    z-index: -1;
    pointer-events: none;
}

.action-buttons button {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    border: none;
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    font-weight: 800;
    font-size: 19px;
    letter-spacing: 1px;
    color: var(--btn-text, rgba(255, 255, 255, 0.95));
    background-image: linear-gradient(135deg, rgba(255,255,255,0.25) 0%, rgba(255,255,255,0) 40%, rgba(0,0,0,0.15) 100%);
    text-shadow: 0 1px 2px rgba(0,0,0,0.4);
    cursor: pointer;
    position: relative;
    z-index: 2;
    transition: transform 0.08s cubic-bezier(0.3, 0.8, 0.3, 1), box-shadow 0.08s cubic-bezier(0.3, 0.8, 0.3, 1);
    outline: none;
}

.btn-a {
    background-color: var(--btn-a);
    box-shadow: 
        inset 0 1.5px 2px rgba(255,255,255,0.6), 
        inset 0 -2px 4px rgba(0,0,0,0.3), 
        0 4px 0 rgba(0,0,0,0.6), 
        0 6px 10px rgba(0,0,0,0.6); 
}
.btn-b {
    background-color: var(--btn-b);
    box-shadow: 
        inset 0 1.5px 2px rgba(255,255,255,0.6), 
        inset 0 -2px 4px rgba(0,0,0,0.3), 
        0 4px 0 rgba(0,0,0,0.6), 
        0 6px 10px rgba(0,0,0,0.6); 
}

.action-buttons .btn-a:active,
.action-buttons .btn-a.pressed {
    transform: translateY(4px);
    box-shadow: 
        inset 0 2px 4px rgba(0,0,0,0.5), 
        inset 0 -1px 2px rgba(255,255,255,0.1), 
        0 0px 0 rgba(0,0,0,0.6),
        0 2px 4px rgba(0,0,0,0.5);
}

.action-buttons .btn-b:active,
.action-buttons .btn-b.pressed {
    transform: translateY(4px);
    box-shadow: 
        inset 0 2px 4px rgba(0,0,0,0.5), 
        inset 0 -1px 2px rgba(255,255,255,0.1), 
        0 0px 0 rgba(0,0,0,0.6),
        0 2px 4px rgba(0,0,0,0.5);
}

.btn-group::after {
    content: 'A';
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    font-weight: 800;
    color: rgba(255,255,255,0.4);
    font-size: 15px;
    letter-spacing: 1px;
    margin-top: 2px;
}
.btn-group:last-child::after { content: 'B'; }
.btn-group:first-child::after { content: 'A'; } 



.speaker {
    grid-area: speaker;
    display: flex;
    justify-content: center;
    gap: 8px;
    transform: rotate(25deg);
    opacity: 0.85; /* Increased opacity for more definition */
    margin-bottom: 20px;
    justify-self: end; /* Align to right side instead of using margin-left % */
    padding-right: 40px;
}

.speaker span {
    width: 6px;
    height: 60px;
    background: rgba(0,0,0,0.6); /* Darker interior for depth */
    border-radius: 5px;
    box-shadow: 
        inset 1px 1px 3px rgba(0,0,0,0.9), /* Deeper internal shadow */
        inset -1px -1px 2px rgba(255,255,255,0.1), /* Rim highlight inside hole */
        1px 1px 1px rgba(255,255,255,0.2); /* Edge highlight on the console body */
}

.console.landscape .speaker {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 6px; /* slightly more gap */
    margin-top: auto; 
    margin-bottom: 25px;
    transform: rotate(-10deg);
    margin-left: 0;
}

.console.landscape .speaker span {
    width: 8px; /* Slightly larger holes */
    height: 8px;
    background: rgba(0,0,0,0.8);
    border-radius: 50%;
    box-shadow: 
        inset 1px 1px 3px rgba(0,0,0,0.9),
        inset -1px -1px 1px rgba(255,255,255,0.1),
        1px 1px 1px rgba(255,255,255,0.2);
}

/* --- Meta Controls (Start / Select) --- */
.meta-controls {
    grid-area: meta;
    display: flex;
    justify-content: flex-start; /* Moved to left */
    padding-left: 20px; /* More to the left */
    gap: 15px;
    margin-bottom: 20px; /* Align with speaker */
    transform: rotate(25deg);
    z-index: 5;
}

.meta-group {
    display: flex;
    justify-content: center;
    position: relative;
    /* Remove column layout/gap to isolate the button size directly */
    width: 60px;
    height: 14px;
    z-index: 1; /* Match A/B grouping */
}

/* Recessed Border (similar to A/B) */
.meta-group::before {
    content: '';
    position: absolute;
    top: -5px; /* Sits exactly 5px above the 14px button */
    left: -5px; /* Sits exactly 5px left of the 60px button */
    width: 70px; /* 60px button + (5px * 2) = 70px */
    height: 24px; /* 14px button + (5px * 2) = 24px */
    border-radius: 12px;
    background: rgba(0,0,0,0.05);
    box-shadow: 
        inset 2px 2px 5px rgba(0,0,0,0.6), 
        inset -1px -1px 3px rgba(255,255,255,0.1),
        0 1px 1px rgba(255,255,255,0.2); 
    z-index: -1;
    pointer-events: none;
}

.pill-label {
    position: absolute;
    top: 24px; /* Pull it completely down below the button + groove */
    width: 100%;
    text-align: center;
    font-size: 10px;
    color: rgba(255,255,255,0.4);
    font-family: sans-serif;
    font-weight: bold;
    letter-spacing: 0.5px;
    /* transition name for labels */
    view-transition-name: none; 
}
.pill-label.label-select { view-transition-name: label-select; }
.pill-label.label-start { view-transition-name: label-start; }


.pill-btn {
    width: 60px;
    height: 14px;
    background: linear-gradient(to bottom, #666, #444);
    border: none;
    border-radius: 10px;
    position: relative;
    box-shadow: 0 2px 5px rgba(0,0,0,0.5);
    cursor: pointer;
    view-transition-name: none; /* Disable individual transition */
}

/* Assign unique view transition names to each button so they track individually */
.pill-btn.select { view-transition-name: btn-select; }
.pill-btn.start { view-transition-name: btn-start; }

.pill-btn:active, .pill-btn.pressed {
    transform: translateY(2px);
    box-shadow: inset 0 2px 5px rgba(0,0,0,0.8);
}


/* Landscape: Top corners of console face */
.console.landscape .meta-controls {
    position: absolute;
    top: 20px;
    bottom: auto; /* Ensure bottom is cleared */
    left: 40px;
    right: 40px;
    width: auto;
    height: auto;
    transform: none;
    justify-content: space-between;
    gap: 0;
    pointer-events: none;
    padding: 0;
    margin: 0;
    z-index: 20;
}

.console.landscape .meta-group {
    flex-direction: column-reverse; /* Put label on top */
    gap: 2px;
}

.console.landscape .meta-group::before {
    top: -3px; /* Adjust for landscape */
    width: 34px; 
    height: 28px;
    border-radius: 14px;
    /* In landscape, buttons are on glass, keep subtlety */
    background: rgba(0,0,0,0.2); 
    box-shadow: inset 1px 1px 3px rgba(0,0,0,0.8);
}

.console.landscape .pill-label {
    font-size: 8px; /* Slightly smaller text */
}

.console.landscape .pill-btn {
    pointer-events: auto;
    width: 24px;
    height: 12px;
    border-radius: 6px;
    background: #333;
    box-shadow: 0 1px 2px rgba(0,0,0,0.8), inset 0 1px 1px rgba(255,255,255,0.1);
}

.console.landscape .pill-btn:active, 
.console.landscape .pill-btn.pressed {
    transform: translateY(1px);
    background: #222;
    box-shadow: 0 0 1px rgba(0,0,0,0.8), inset 0 2px 4px rgba(0,0,0,0.5);
}

/* Responsiveness */
@media (max-width: 500px) {
    body {
        padding: 0;
        margin: 0;
        background-color: #000;
        background-image: none;
        overflow: hidden;
    }

    .console {
        width: 100vw;
        border-radius: 0;
        height: 100dvh;
        min-height: 100dvh;
        transform: none;
        box-shadow: none;
        border: none;
        margin: 0;
    }

    .top-hud {
        left: 10px;
        right: 52px;
        gap: 10px;
        font-size: 11px;
    }

    .profile {
        margin-top: 58px;
    }

    .help-panel {
        font-size: 16px;
    }

    /* Mobile: Hide power switch entirely */
    .power-switch-container {
        display: none !important;
    }
}

@media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
    }
}

/* --- App Turn Button (Floating Action) --- */
.app-turn-control {
    position: fixed;
    bottom: 30px;
    right: 30px;
    z-index: 1000;
    pointer-events: auto;
}

.app-btn {
    appearance: none;
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: rgba(20, 20, 30, 0.85);
    backdrop-filter: blur(8px);
    color: rgba(255, 255, 255, 0.9);
    border: 2px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 10px 25px rgba(0,0,0,0.6);
    display: flex;
    justify-content: center;
    align-items: center;
    cursor: pointer;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    outline: none;
}

.app-btn:hover {
    transform: scale(1.1) rotate(5deg);
    background: rgba(40, 40, 50, 0.95);
    color: #fff;
    box-shadow: 0 12px 35px rgba(108, 38, 194, 0.5); /* Brand Purple Glow */
    border-color: rgba(255, 255, 255, 0.3);
}

.app-btn:active, .app-btn.pressed {
    transform: scale(0.9);
}

.turn-icon {
    width: 28px;
    height: 28px;
    fill: currentColor;
    transition: transform 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.app-btn:hover .turn-icon {
    transform: rotate(180deg);
}

@media (max-width: 600px) {
    .app-turn-control {
        bottom: 20px;
        right: 20px;
    }
    
    .app-btn {
        width: 52px;
        height: 52px;
    }
    
    .turn-icon {
        width: 22px;
        height: 22px;
    }
}

</style>
